#Initialize data tables, fill with test data
#(to be removed once connected to node and running full tests),
#then display the created tables

#most functionality has been removed at this point.....

psql -f init.sql
